# This server scrapes immowelt.de directly — no API key or proxy required.
REALESTATE_FREE_LIMIT=10
REALESTATE_UPGRADE_URL=https://example.com
# Optional: append each tool call to a JSONL file (your usage metric)
# REALESTATE_USAGE_LOG=usage.jsonl
